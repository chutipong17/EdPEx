BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[user] ALTER COLUMN [last_name] NVARCHAR(255) NULL;
ALTER TABLE [dbo].[user] ALTER COLUMN [mobile_number] VARCHAR(20) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
